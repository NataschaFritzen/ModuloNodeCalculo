EDITAR PEDIDO
form(role='form' method='POST' action='/pedidos/editar/' + produto.id)
  div.form-group
    label(for='id_cliente') Id do cliente:
    input#nome.form-control(type='number', name='id_cliente', value=pedido.id_cliente)

  div.form-group
    label(for='id_produto') Id do produto:
    input#descricao.form-control(type='number', name='id_produto', value=pedido.id_produto)

  div.form-group
    label(for='quantidade') Quantidade:
    input#valor.form-control(type='number', name='quantidade', value=pedido.quantidade)

  button.btn.btn-primary(type="submit", name="asdf") Editar
